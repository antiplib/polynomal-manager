#include "base.h"

extern node* add(std::string str);

extern node* solve(std::string str);

extern node* addition(node* y, node* x);

extern node* multiplicate(node*y, node *x);

extern int n_of_powers(std::vector <int> array);

extern std::string cout_node(node* n);

extern void bubble_sort(node*& a);

extern std::vector <int> search_an_solution(polynomal &first);

extern void search_derivative(polynomal &f, int number);

extern base *baza;

extern int cnt_of_bases;

extern std::string alf;

extern node *den(node *aa, node* bb);

extern int get_pow(node*a);

extern node* un_multiplicate(node* b);

extern void un_bubble_sort(node*&a);

